diff --git a/deploy/base/kustomization.yaml b/deploy/base/kustomization.yaml
index d810efe..367e19f 100644
--- a/deploy/base/kustomization.yaml
+++ b/deploy/base/kustomization.yaml
@@ -3,4 +3,3 @@ kind: Kustomization
 
 resources:
   - redis.yaml
-  - monitoring.yaml
diff --git a/deploy/neutral/full-mode-scheduling/load-balance/gateway.yaml b/deploy/neutral/full-mode-scheduling/load-balance/gateway.yaml
index 4cb8170..84d8ffd 100644
--- a/deploy/neutral/full-mode-scheduling/load-balance/gateway.yaml
+++ b/deploy/neutral/full-mode-scheduling/load-balance/gateway.yaml
@@ -34,35 +34,6 @@ spec:
         role: gateway
     spec:
       terminationGracePeriodSeconds: 300
-      initContainers:
-      - name: download-tokenizer
-        image: llumnix-registry.cn-beijing.cr.aliyuncs.com/llumnix/python3:3.12-slim-bookworm
-        command: ["/bin/sh", "-c"]
-        args:
-        - |
-          pip3 install modelscope -i https://mirrors.aliyun.com/pypi/simple/ && \
-          pip3 install packaging -i https://mirrors.aliyun.com/pypi/simple/ && \
-          python3 << 'EOF'
-          from modelscope import snapshot_download
-          
-          model_dir = snapshot_download(
-              'Qwen/Qwen3-30B-A3B-FP8',
-              cache_dir='/tokenizers',
-              allow_patterns=['tokenizer.json', 'tokenizer_config.json']
-          )
-          
-          print(f"download tokenizer done: {model_dir}")
-          EOF
-        volumeMounts:
-        - name: tokenizer-storage
-          mountPath: /tokenizers
-        resources:
-          requests:
-            memory: "512Mi"
-            cpu: "200m"
-          limits:
-            memory: "1Gi"
-            cpu: "1000m"
       containers:
       - name: gateway
         image: ${REPOSITORY}/gateway:${GATEWAY_IMAGE_TAG}
@@ -88,9 +59,9 @@ spec:
         - "--scheduling-policy"
         - "load-balance"
         - "--tokenizer-path"
-        - "/tokenizers/Qwen/Qwen3-30B-A3B-FP8"
+        - "/hf-cache/hub/models--meta-llama--Meta-Llama-3-8B-Instruct/snapshots/8afb486c1db24fe5011ec46dfbe5b5dccdb575c2"
         - "--max-model-len"
-        - "40960"
+        - "8192"
         - "--enable-full-mode-scheduling=true"
         ports:
         - containerPort: 8089
@@ -116,9 +87,11 @@ spec:
           initialDelaySeconds: 5
           periodSeconds: 5
         volumeMounts:
-        - name: tokenizer-storage
-          mountPath: /tokenizers
+        - name: hf-cache
+          mountPath: /hf-cache
           readOnly: true
       volumes:
-      - name: tokenizer-storage
-        emptyDir: {}
+      - name: hf-cache
+        hostPath:
+          path: /NHNHOME/huggingface
+          type: Directory
diff --git a/deploy/neutral/full-mode-scheduling/load-balance/neutral.yaml b/deploy/neutral/full-mode-scheduling/load-balance/neutral.yaml
index f174993..221a475 100644
--- a/deploy/neutral/full-mode-scheduling/load-balance/neutral.yaml
+++ b/deploy/neutral/full-mode-scheduling/load-balance/neutral.yaml
@@ -10,20 +10,16 @@ spec:
       metadata:
         labels:
           llumnix.io/infer-type: "neutral"
-          llumnix.io/model: "Qwen3-30B-A3B-FP8"
+          llumnix.io/model: "Meta-Llama-3-8B-Instruct"
       spec:
         terminationGracePeriodSeconds: 30
         containers:
         - name: vllm
           image: ${REPOSITORY}/vllm:${VLLM_IMAGE_TAG}
           securityContext:
-            capabilities:
-              add:
-              - IPC_LOCK
-              - SYS_RAWIO
             runAsGroup: 0
             runAsUser: 0
-          imagePullPolicy: Always
+          imagePullPolicy: IfNotPresent
           command:
             - /bin/bash
             - -c
@@ -33,36 +29,19 @@ spec:
               find /dev/shm -type f -delete
 
               #################
-              # RUN vLLM decode worker
+              # RUN vLLM (dense model: Llama-3-8B-Instruct, one independent instance per local GPU)
               #################
-              START_RANK=$(( ${LWS_WORKER_INDEX:-0} * DP_SIZE_LOCAL ))
-
-              # --data-parallel-hybrid-lb: Use exernal load balancing across nodes, and internal load balancing within a node
-              # --enable-expert-parallel:  Use TPxDP in attention, EP in MoE layers
-              # --async-scheduling: Reduce white space between engine steps
-              # --enable-dbo:  Dual batch overlap (DBO) overlaps compute with collective communication.
-              # --enable-eplb: Expert-parallel load balancing reduces EP load imbalance by replicating heavily-used experts Performance-memory tradeoff: on DeepSeekV3 eplb uses an extra 2GB per redundant expert per GPU Divisibility constraint: num_routed_experts (256 for DSv3) + num_redundant_experts must be divisible by the number of GPUs.
-
-              # Launch multiple vLLM servers in parallel
+              # Launch one vLLM server per local GPU (TP=1). No MoE/EP/DeepEP/DP — this box has no RDMA.
               for i in $(seq 0 $((DP_SIZE_LOCAL - 1))); do
-                RANK=$((START_RANK + i))
                 PORT=$((8000 + i))
-                
+
                 CUDA_VISIBLE_DEVICES=$i vllm serve \
-                  Qwen/Qwen3-30B-A3B-FP8 \
+                  meta-llama/Meta-Llama-3-8B-Instruct \
                   --port $PORT \
-                  --trust-remote-code \
-                  --data-parallel-external-lb \
-                  --enable-expert-parallel \
                   --tensor-parallel-size $TP_SIZE \
-                  --data-parallel-size $((LWS_GROUP_SIZE * DP_SIZE_LOCAL)) \
-                  --data-parallel-rank $RANK \
-                  --data-parallel-address ${LWS_LEADER_ADDRESS} \
-                  --data-parallel-rpc-port 5555 \
+                  --max-model-len 8192 \
+                  --gpu-memory-utilization 0.6 \
                   --async-scheduling \
-                  --kv-cache-memory-bytes=${KV_CACHE_MEMORY_BYTES-} \
-                  --gpu-memory-utilization 0.7 \
-                  --compilation_config '{"level":"0", "cudagraph_mode":2}' \
                   &
               done
 
@@ -86,21 +65,15 @@ spec:
           - name: LLUMNIX_INSTANCE_TYPE
             value: "neutral"
 
-          # download models
+          # model source: pre-downloaded HuggingFace cache on Lustre, offline (no token, no download)
           - name: VLLM_USE_MODELSCOPE
-            value: "true"
-
-         # VLLM_MOE_DP_CHUNK_SIZE is a tunable parameter.
-         # Higher values increase the memory footprint of the hidden states in the MoE layers,
-         # which decreases the available memory for KV Cache.
-         # Lower values may cause poor performance, especially in load-imbalanced scenarios.
-         # The value 384 was chosen to be greater than the concurrency expected to see on any DP rank.
-         # If you increase the VLLM_MOE_DP_CHUNK_SIZE above 510 you will also need to increase the
-         # default NVSHMEM_QP_DEPTH to be at least 2 * (chunk_size + 1), which will increase the
-         # memory NVSHMEM allocates up front.
-         # Has no effect when VLLM_ALL2ALL_BACKEND=deepep_high_throughput.
-          - name: VLLM_MOE_DP_CHUNK_SIZE
-            value: "384" # vLLM default is 256
+            value: "false"
+          - name: HF_HUB_OFFLINE
+            value: "1"
+          - name: TRANSFORMERS_OFFLINE
+            value: "1"
+          - name: HF_HUB_CACHE
+            value: /hf-cache/hub
           - name: DP_SIZE_LOCAL
             value: "4"
           - name: TP_SIZE
@@ -109,30 +82,12 @@ spec:
             value: /usr/lib64
           - name: VLLM_SKIP_P2P_CHECK
             value: "1"
-          - name: VLLM_RANDOMIZE_DP_DUMMY_INPUTS
-            value: "1"
-          - name: VLLM_USE_DEEP_GEMM
-            value: "1"
-          - name: VLLM_ALL2ALL_BACKEND
-            value: deepep_low_latency
-          - name: NVIDIA_GDRCOPY
-            value: enabled
-          - name: NVSHMEM_REMOTE_TRANSPORT
-            value: ibgda
-          - name: NVSHMEM_IB_ENABLE_IBGDA
-            value: "true"
-          - name: NVSHMEM_BOOTSTRAP_UID_SOCK_IFNAME
-            value: eth0
           - name: GLOO_SOCKET_IFNAME
             value: eth0
           - name: NCCL_SOCKET_IFNAME
             value: eth0
           - name: VLLM_LOGGING_LEVEL
-            value: DEBUG
-          - name: VLLM_NIXL_SIDE_CHANNEL_HOST
-            valueFrom:
-              fieldRef:
-                fieldPath: status.podIP
+            value: INFO
 
           # Use cache directories from the mounted volume under one easy to mount
           # root directory.
@@ -144,9 +99,6 @@ spec:
             value: /var/cache/vllm/vllm
           - name: FLASHINFER_WORKSPACE_BASE
             value: /var/cache/vllm/flashinfer
-          # HuggingFace is likely to be quite a bit larger, give it its own cache
-          - name: HF_HUB_CACHE
-            value: /var/cache/huggingface
 
           ports:
           - containerPort: 8000
@@ -174,6 +126,10 @@ spec:
             periodSeconds: 10
             timeoutSeconds: 5
             failureThreshold: 3
+          volumeMounts:
+          - name: hf-cache
+            mountPath: /hf-cache
+            readOnly: true
           resources:
             limits:
               ephemeral-storage: 256Gi
@@ -211,4 +167,9 @@ spec:
               valueFrom:
                 fieldRef:
                   fieldPath: status.podIP
+        volumes:
+        - name: hf-cache
+          hostPath:
+            path: /NHNHOME/huggingface
+            type: Directory
   
\ No newline at end of file
