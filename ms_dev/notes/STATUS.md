# 지금 무엇을 하고 있는가 — llumnix_reproduce

이 파일은 **상태**만 담는다. 다음 실험이 끝나면 바뀌는 것들이다.
바뀌지 않는 규칙(함정·판정 규칙·서술 규칙)은 [CLAUDE.md](../../CLAUDE.md)에 있다.

**이 파일은 [fluidserve-implementation.md](fluidserve-implementation.md)의 캐시다.**
두 문서가 어긋나면 implementation.md가 정본이고, 그 문서는 **끝이 항상 가장 최신**이다.

**갱신 규칙**: 실험이 끝나면 §1을 고치고 §3에 행을 추가한다. `exp-record` 스킬에 같은 규칙이 있다.

---

## §1 지금 상태 (2026-08-09 02:40 KST 확인)

**목표**: llm-d보다 SLO 달성률(offered·admitted)과 token goodput이 높을 것.

**현재 버전은 [fluidserve-v0.2](fluidserve-v0.2.md)** (2026-08-08, 바이너리 `a96dac12`,
파드에서 검증). v0.1.2와 코드가 같고 **`--fluidserve-prefix-aware`의 기본값만 켜졌다.**

⚠ **arm 이름 규약이 바뀌었다.** `fspfx` = 배포 기본 설정(prefix 켬), `fluidserve` =
prefix를 끈 ablation. **네 드라이버의 `fluidserve` arm에 `FS_PREFIX=false`를 명시로 박아서**
기존 `results/*_fluidserve_*`와 같은 설정을 유지한다 — 안 그러면 한 arm 이름이 v0.2 전후로
다른 설정을 뜻하게 된다. **논문의 "FluidServe"는 `fspfx` 수치다.**

**정본**: prefix 작업 [fluidserve-prefix.md](fluidserve-prefix.md) · 워크로드 결정
[workload-decision.md](workload-decision.md) · llm-d [llmd-baseline.md](llmd-baseline.md) §9.9 ·
실험 `experiments/EXP-66/67/68_*.md`.

---

### ⚠ 워크로드가 2026-08-08 11:00에 바뀌었다 — 그 전후를 나란히 놓으면 안 된다

`run_experiment.py:_worker_main`의 워커별 데이터셋 분할이 mixed 워크로드에서 **한 번도 돈 적이
없었다**(`isinstance(dataset, list)`인데 dict를 돌려준다). 워커 12개가 같은 프롬프트 열을
보내서 **모든 프롬프트가 정확히 12.00번씩, 사본 간격 0.11초, 전체 2.1초 안에** 나갔다.
고쳤고(`0cef289`) chat 풀을 1,000 → 10,000 대화로 올렸다.

smoke 검증(45 req/s 3분): **재생 12.00× → 1.00×**, **엔진 prefix hit 83~86% → 28.9%**,
입력 토큰 평균은 안 움직임(chat 673 → 677).

→ **EXP-53·57·66·67의 값은 이 수정 이전 워크로드다. 이후 값과 같은 표에 넣지 않는다.**

### 끝난 것: EXP-68 sweep (35/45/55 req/s) — **12조건 두 반복 완료, 16:55 KST**

`results/aggregate_analysis/exp68sweep`(그림 + README). 요청 단위, 괄호는 두 반복의 폭:

| req/s | offered **fspfx** | offered llm-d | 차이 / 큰 반복 폭 | goodput **fspfx** / llm-d | 거절률 **fspfx** / llm-d |
|---|---|---|---|---|---|
| 35 | **77.6** (76.9~78.4) | 42.9 (**35.2~50.6**) | **+34.7** / 15.4 | **13,147** / 8,295 | **16.0%** / 51.8% |
| 45 | **59.4** (59.4~59.5) | 24.9 (24.2~25.7) | **+34.5** / 1.5 | **13,003** / 6,940 | **35.4%** / 71.5% |
| 55 | **48.3** (47.4~49.1) | 21.0 (18.1~23.8) | **+27.3** / 5.7 | **13,184** / 7,168 | **46.3%** / 76.5% |

**차이가 두 arm 중 큰 반복 폭의 2.3~23배**라 잡음이 아니다. admitted 분모에서도 우리가
앞서거나 같다(+3.3 / +4.1 / +0.3). cutoff를 전부 실패로 세는 최악의 경우에도 +33.7 /
+33.7 / +26.7로 유지된다. <!-- numbers-ok: 26.7은 EXP-68의 점수 차이이고 철회 표의 EXP-53 PolyServe 포화 도착률 26.7과 무관하다 -->

**기제**: llm-d가 chat(요청의 76.9%, 입력 677 토큰으로 가장 짧다)을 56/80/83% 거절하고 긴
클래스를 남겨서, prompt 토큰은 우리와 비슷한데 생성 토큰이 절반이다. 엔진 계층에서는 네
엔진의 실행 배치 합이 318 대 우리 775이고 한 엔진이 구간의 5.7%를 배치 5건 미만으로 보낸다
— **받을 수 있는 엔진을 비워 둔 채 76.5%를 거절한다.** 우리 쪽 비대칭은 종류가 달라서 엔진
8000이 KV 99.5%로 포화된 채 20.3건을 큐에 세우고 나머지 셋은 44~51%에 큐가 없다(EXP-67b의
gate 병목과 같은 방향). 클래스 분리는 수정 뒤에도 남았다 — 유효 인스턴스 2.93 대 3.60.

⚠ **llm-d의 35 req/s 반복 폭 15.4점은 잡음이 아니라 자르는 클래스가 바뀐 것이다** — 반복 1은
chat을 43.2% 거절하고 반복 2는 68.8% 거절한다. 예측기가 조건마다 온라인 학습하는 것이
원인일 가능성이 높은데 **직접 확인하지 않았다**(각 run의 `training_server.log`로 가능).

전체는 `experiments/EXP-68_workload-deduplication.md` §8.

**⚠ 집계를 밝힌다**: 위는 **요청 단위**다. `exp22_fluidserve.py` 표가 첫 열에 찍는
`eqmix_off`는 **클래스 균등 평균**이고 값이 크게 다르다(35에서 64.9 대 77.6). llm-d가
거절하는 것이 주로 chat인데 chat이 요청의 76.9%라 두 집계가 갈라진다. EXP-68 §8.0.2.

**채점**(PRERUN 디렉토리를 반드시 뺀다 — 글롭에 섞인다):
```bash
python3 analysis_scripts/request_level/exp22_fluidserve.py   --runs $(ls -d results/*exp68sr*_rpm_* | grep -v PRERUN)   --out-dir results/aggregate_analysis/exp68sweep
```

### 끝난 것: **믹스가 움직인다는 전제에 실측 근거가 생겼다** (2026-08-09 00:00 KST)

Azure LLM Inference 2024의 conv와 code가 별도 trace이므로 **둘의 비가 곧 두 요청 집단의
구성비**다. 두 파일 전량(conv 27,303,999건 / code 16,803,695건)을 셌다.

| | 최소 | 최대 | 폭 |
|---|---|---|---|
| code의 요청 비중, 시각대별(UTC) | 08시 **12.0%** | 20시 **56.7%** | **4.71배** |
| code의 입력 토큰 비중, 시각대별 | 17.8% | 69.0% | **3.87배** |

**시각대별 표가 08시 최소에서 20시 최대로 단조 증가하는 단봉이라 잡음이 아니라 일주기
패턴이다.** 우리 합성 스케줄의 폭이 같은 자릿수다(swe 입력 토큰 비중 14~57% = 4.07배).

→ **motivation §12 항목 5(가장 큰 위험)의 절반이 닫혔다** — 변동 폭은 실측 근거가 생겼고
**수준(10:2:1)은 여전히 우리가 고른 값**이다. 정본은 motivation.md **§7.3.1**,
스크립트는 `traces/azure/analyze_mix_over_time.py`.

⚠ **"우리 믹스가 Azure의 믹스다"라고 쓰면 안 된다** — conv가 chat, code가 swe에 가깝고
**deepresearch에 해당하는 것이 Azure에 없다.** 쓸 수 있는 것은 "실제 서비스에서 두 요청
집단의 구성비가 하루에 4.71배 움직인다"까지다.

### 끝난 것: EXP-70 무릎 탐침 — **격자가 정해졌다** (2026-08-09 02:32 KST, 7/8)

정본 `experiments/EXP-70_knee-probe.md`, 그림 `results/aggregate_analysis/exp70_knee`.
**⚠ 1반복이므로 값을 결과로 인용하지 않는다 — 격자를 고르는 데만 쓴다.**

| req/s | v0.2 | llm-d | | req/s | v0.2 | llm-d |
|---|---|---|---|---|---|---|
| 10 | **99.8** | 95.8 | | 35 | 77.6 | 42.9 |
| 15 | **99.9** | (재실행) | | 45 | 59.4 | 24.9 |
| 20 | **99.6** | 88.7 | | 55 | 48.3 | 21.0 |
| 25 | **95.7** | 72.4 | | 70 | 38.2 | 17.7 |

**무릎(90% 통과, 선형보간): v0.2 = 28.1 req/s, llm-d = 18.2 req/s.**

**제안 격자 `10, 15, 20, 25, 30, 40, 55, 70`** — 등간격이 아니라 무릎 근처를 촘촘하게.
10~30에 다섯 점이 들어가고 두 arm의 무릎이 다 그 안이다. 80은 뺐다(70에서 이미 38.2/17.7).

- **llm-d는 10 req/s에서도 95.8이고 2.1%를 거절한다** — 저부하에서도 100%에 못 간다.
  EXP-66(수정 전 워크로드, 2반복)의 94.8~98.5와 같은 방향이라 재현이다.
- **goodput은 35 req/s부터 13,000 근처에서 평평하다.** 그 위에서 달라지는 것은 거절률뿐이다.
- ⚠ **나머지 세 baseline의 무릎은 모른다.** PolyServe·부하분산은 10에서도 100%가 아닐 수
  있다. **5 baseline sweep 전에 그 셋을 10 req/s에서 한 번씩 재는 것이 3조건 1시간**이고
  80조건을 다시 도는 것보다 싸다.
- ⚠ **swe 재생이 50 req/s부터 시작한다**(워커당 draw 154 대 풀조각 125). 격자의 55·70에서
  swe만 프롬프트가 다시 나간다. transcript가 1,500건뿐이라 그렇고, 한 시간 trace를 막고
  있는 것과 같은 원인이다.

**사고 하나**: `llm-d @15`가 `ABORT: engines not serving after restart`로 빠졌다(함정 F,
우리 변경과 무관). 체인이 건너뛰고 진행했고 데이터 오염은 없다. 재실행 중.

### 끝난 것: EXP-69 대조군### 끝난 것: EXP-69 대조군 — **prefix 인식은 우위의 30~39%이고 나머지는 기본 정책이다**

`fluidserve`(prefix 없음) 6조건, 20:19 KST 완료. `fspfx`와 **환경변수 `FS_PREFIX` 하나만
다르고** 바이너리는 같은 `5a572dc2`다. 정본은 `experiments/EXP-69_prefix-control.md`,
그림은 `results/aggregate_analysis/exp69_control`.

| req/s | `fluidserve` 대조 | `fspfx` | **prefix의 이득** | llm-d | 기본 정책의 몫 |
|---|---|---|---|---|---|
| 35 | 64.2 | **77.6** | **+13.5** | 42.9 | **+21.2** |
| 45 | 49.3 | **59.4** | **+10.2** | 24.9 | **+24.3** |
| 55 | 38.0 | **48.3** | **+10.3** | 21.0 | **+17.0** |

**대조군이 prefix 인식 없이도 llm-d를 세 rate 전부에서 앞선다.** goodput +13.9~21.7%,
거절 −7.4~9.2%p. EXP-68의 H3이 닫혔다.

⚠ **기제가 "캐시 있는 곳으로 보낸다"가 아니다.** 엔진 prefix hit rate가 45/55에서 오히려
1.5%p 낮은데(23.9 → 22.3, 24.8 → 23.3) 거절이 7%p 줄고 토큰은 10% 늘었다. **prefill 비용을
인스턴스별로 정확히 매기게 되어 admission이 덜 보수적이 된 것**이다. prefix는
`sortCandidates` 점수에 안 들어가고 feasibility·TTFT 판정에만 들어간다.
→ **"prefix-aware routing"이라고 쓰면 안 된다.** "prefill 비용을 인스턴스별로 정확히 매긴다"다.

⚠ **task당 유효 엔진 수는 이 워크로드에서 어느 방향으로도 못 잰다** — 수정 전에는 사본 12개가
동시에 와서, 수정 후에는 task가 요청을 하나만 내서다(55 req/s에서 요청 2개 이상인 task가
8~92개뿐). EXP-67 H2의 값을 인용하면 안 된다.

### 첫 탐침 결과 — 70 req/s에서 **뒤집혔다** (EXP-68 §4, 4조건 완료)

| 70 req/s (2반복 평균, 괄호는 폭) | **fspfx** | llm-d |
|---|---|---|
| **offered 달성률, 요청 단위** | **38.2** (36.9/39.4) | 17.7 (17.0/18.3) |
| admitted, 요청 단위 | 88.4 | 92.1 |
| **token goodput** | **13,054** | 7,347 |
| 거절률 | 54% | **77%** |
| 엔진 prefix hit | 26.9% | 36.7% |
| (참고) offered, 클래스 균등 | 32.7 | 25.5 |

EXP-66에서 같은 rate가 요청 단위로 51.9 대 57.9(**−6.0**)였으니 **부호가 바뀌고 24.5점이
움직였다.** ⚠ EXP-66 값은 수정 이전 워크로드라 차이의 크기는 두 원인이 섞인 것이다.
→ **llm-d의 70 req/s 우위는 prefix 재사용에서 온 것이 맞았다**(§9.11 확증).
→ **예열 주행은 효과가 없다** — llm-d 측정 주행의 분당 hit이 `24 46 45 37 36 34…`로 첫 1분이
오히려 낮다. 재생 증폭이 사라지니 예열의 이점도 사라졌다.

⚠ **조심해서 읽을 것 둘**: ① 양쪽 다 절대값이 무너졌다(요청 단위로 fspfx 51.9 → 38.2,
llm-d 57.9 → 17.7,
함대 총 출력 −25%) ② **70은 이제 훨씬 깊은 과부하**라 라우팅이 아니라 admission의 공격성을
재고 있을 수 있다. **그래서 35/45/55를 재는 중이다.**

### 밤사이 끝난 것 (전부 수정 이전 워크로드)

- **EXP-66 두 반복** — 교차점이 45다. 15/25/35는 우리가 100.0 대 94.8~98.5로 이기고, 45 무승부,
  50~60에서 llm-d +9.7~13.0, **70은 확립 안 됨**(llm-d 반복 폭 11.7).
- **EXP-67 (prefix 인식, 12조건)** — 45/55/70에서 **+11.0 / +7.8 / +2.6**, goodput +6.7~11.7%,
  거절 −80/−30/−22%. H1 통과. **H2 실패는 워크로드 때문**(12개 사본이 동시에 오면 흩을 수밖에
  없다). goodput만 보면 세 rate 전부 llm-d와 같거나 앞섰다.
- **EXP-67b (κ 되먹임 수정)** — **음성.** κ는 의도대로 내려갔는데(0.55 → 0.41) 달성률이
  72.4 대 72.5로 안 움직인다. **infeasible 사유가 gate 92~96%, memory 0%** —
  **prefill을 정확히 만들고 나니 gate가 병목이다.**

### 배포 상태

- 스케줄러 바이너리 **`5a572dc2`**(κ 고친 판, 파드에서 `md5sum /proc/1/exe`로 검증).
  백업 `bin-backup/scheduler-exp07.pre-exp67`(원본 `c2d970ca`), `.pre-exp67b`.
- prefix 인식은 **기본 꺼짐**. arm이 `FS_PREFIX=true`로 켠다.
- llm-d 스택 다섯 파드가 엔진과 같은 노드에 떠 있다(EXP-53/57에는 없던 배경).

### 결정이 필요한 것

1. ~~대조군 `fluidserve`~~ — **닫혔다 (EXP-69)**. prefix 인식이 +13.5 / +10.2 / +10.3점이고
   기본 정책이 나머지 +21.2 / +24.3 / +17.0점이다.
2. **한 시간 trace** — 아직 못 고쳤다. chat 42,357 대화, **swe transcript 18,154건**이 필요한데
   short7k는 1,500건뿐이다. 새로 만드는 것이 별도 작업.
3. **믹스 비율과 swe 시스템 프롬프트** — 같은 단계에서 안 바꾸기로 했다. "agent 약 60%"에
   닿으려면 후자가 필요하다(재생 0에서도 구조적 공유가 73.2%).
4. **gate 축** — EXP-67b가 가리킨 새 병목. `gateSlack`·`ownBudgetGate`(EXP-46/50/52)를 다시 볼
   값어치가 있다.

**최근 끝난 것 둘**:
- **EXP-62**(04:09 KST, 18조건) — **QoServe가 PolyServe에서는 도움이 된다**(+0.98 / +2.65 /
  +2.79), Llumnix에서 해로웠던 것과 부호가 반대다. **이득이 전부 chat**이고 엔진별 큐가 네
  조건 전부 한 엔진 2,293~2,746건 · 나머지 셋 0으로 QoServe가 그 비대칭을 못 바꾼다.
  같은 세션에 **FluidServe 정적 3점**(45 → 94.76, 50 → 78.42, 60 → 59.99). §64.
- **EXP-63**(15:05 KST, 10조건) — **사전 등록한 H1·H2는 통과했는데 재려던 것을 재지
  못했다.** §65.

**EXP-63을 어떻게 읽어야 하나 (중요)**:
- ×0.5가 −8.23점, ×0.7이 +3.90, ×1.5가 +4.20, **×2.0이 +5.59**(반복 범위 안 겹침).
  즉 **적합값 ×1.0이 최선이 아니다.** 그리고 ×1.0은 워크로드에 잘 맞는다(세 부하에서
  실측 dr 평균 975 / 972 / 960 대 프로파일 985, 2.5% 안).
- **원인은 정확도가 아니라 분리 세기다.** 배수를 올리면 chat 유효 인스턴스가 3.10 → 2.72로
  단조 감소하고(−0.897) chat 없는 인스턴스-시각이 1.9 → 20.5%로 오르는데(+0.942) **수요는
  안 움직인다.** 점수를 예측하는 것은 배수(+0.72)가 아니라 분리(−0.955)다.
- **한 클래스만 스케일하면 클래스 간 상대 가중치가 바뀐다.** 총계 변화의 거의 전부가
  chat(비중 0.769)에서 온다 — ×2.0의 +5.59 중 +5.23이 chat이다.
- ⚠ **쓰면 안 되는 문장 둘**: "프로파일이 ±30% 어긋나면 4점 잃는다"(얻는다), "과대추정하면
  좋아진다"(의도한 효과가 아니라 클래스 재가중의 부수 효과라 믹스가 바뀌면 뒤집힌다).
- ✅ **쓸 수 있는 것**: **길이 프로파일은 측정값이자 정책 상수다.** 정직하게 재적합하면
  분리 세기가 같이 바뀌므로 **5.6점이 조용히 사라질 수 있다.**

**EXP-66 (llm-d 기준선) — rep 1 완료, ⚠ llm-d가 앞선다**

**정본은 `ms_dev/notes/llmd-baseline.md`이고 §9.11(축 3의 답 = prefix cache)·§9.12(예측기가
정확히 무엇을 예측하는가)가 가장 최근이다.** 그 앞이 §9.9(rep1 결과)·§9.10(축 4·5·7)·
§9.7(축 여덟). 실험 설계는 `experiments/EXP-66_llm-d-baseline.md`, 그림은
`results/aggregate_analysis/exp66r1/README.md`.

**두 반복 완료 (rep 2가 2026-08-08 02:29 KST에 끝났다).** 요청 단위 offered 달성률과 token
goodput, 두 반복의 평균(괄호는 두 반복의 폭). FluidServe는 EXP-53의 같은 arm 두 반복이다.

| req/s | FluidServe | goodput | llm-d | goodput | 차이 |
|---|---|---|---|---|---|
| 15 | **100.0** (0.0) | 8,054 | 98.5 (3.0) | 7,882 | −1.5 |
| 25 | **100.0** (0.0) | 13,468 | 95.9 (0.2) | 12,884 | −4.1 |
| 35 | **100.0** (0.0) | 17,900 | 94.8 (0.5) | 17,302 | −5.2 |
| 45 | 90.2 (0.5) | 20,462 | 90.5 (1.8) | 20,988 | +0.2 |
| **50** | 77.5 (0.8) | 19,998 | **87.2** (0.2) | 22,373 | **+9.7** |
| **55** | 66.3 (0.1) | 19,090 | **79.3** (0.6) | 21,980 | **+13.0** |
| **60** | 60.9 (**11.7**) | 19,430 | **72.9** (0.2) | 22,668 | **+12.1** |
| 70 | 51.9 (0.7) | 19,435 | 57.9 (**11.7**) | 21,345 | +6.0 |

**rep 1만 볼 때와 이야기가 다르다:**
- **45 req/s 아래에서는 우리가 이긴다** — 세 rate 전부 100.0 대 94.8~98.5이고 goodput도 높다.
  경쟁이 없을 때 llm-d가 1.5~5.2점을 잃는다. rep 1은 15 req/s에서 100.0이었다.
- **45는 무승부**(90.2 대 90.5, llm-d의 반복 폭 1.8 안).
- **50~60에서 llm-d가 9.7~13.0점 앞선다** — 여기가 진짜 격차다.
- **70에서는 확립되지 않았다.** 차이 +6.0인데 **llm-d의 두 반복이 63.7과 52.0으로 11.7점
  벌어진다.** FluidServe도 60에서 66.7과 55.0으로 11.7점 벌어진다. **두 시스템 다 그 근처에서
  불안정하다.**

**격차는 거의 전부 swe다.** 55 req/s에서 클래스별 offered(두 반복 평균):

| 55 req/s | chat | dr | **swe** | 거절률 |
|---|---|---|---|---|
| FluidServe | 66.2 | 86.4 | **30.7** | 24.6% |
| llm-d | 77.3 | 89.2 | **80.2** | 7.9% |

**우리가 3배 더 거절한다.** 그리고 **swe는 두 시스템이 다른 예산을 받는 유일한 클래스**다
(우리 m1 + `25:e2e:30000`, llm-d m1f의 2,500/52).

⚠ **아직 인용하면 안 되는 이유 셋**(넷이었는데 하나 해제): ① 반복 1회
② ~~예열 주행~~ → **2026-08-08에 재고 해제(§9.11)** ③ 세션이 다름(08-02 대 08-07)
④ m1f 대 m1.

**채점은 다시 확인했다 (2026-08-08).** rpm 4200을 원 CSV에서 독립적으로 다시 계산해
66.1015로 재현했다(요약 66.101473). 확인한 것들: 창(도착 60~522초, 70.0 req/s로 측정 목표와
일치), 클래스별 개수 합이 n과 일치, `stream_fallback_used`가 여덟 조건 전부 0~3건.
**주의 하나** — llm-d의 거절은 클라이언트가 `is_rejected`와 `is_error`를 **둘 다** 세우므로
요약표의 `rejected`와 `errored` 열이 같은 요청을 두 번 보고한다. 점수는 OR이라 영향이 없고
**진짜 오류는 0건**이다. 거절 사유가 `KV_THRESHOLD`로 찍히는 것도 Llumnix용 라벨이 llm-d
거절에 붙은 것일 뿐이다.

**축 검사 결과**: 축 4(못 지키는 요청 누적 되먹임) **지지 안 됨 — 진동한다**,
축 5(메모리 제약 없음 → preemption) **지지 안 됨 — 이 워크로드는 KV가 병목이 아님**,
축 7(거절 예외 조항) **지지됨 — 45까지 0.5%다가 50부터 꺾임**(§9.10).
**축 3 = 답했다(§9.11): 원인은 클래스 분리가 아니라 prefix cache 재사용이다.**
- llm-d는 클래스를 거의 안 가른다. 클래스당 유효 인스턴스 수가 70 req/s에서 **3.75**인데
  FluidServe는 **2.41**이다. 부하가 오를수록 llm-d는 퍼지고 우리는 모인다.
- 엔진이 보고하는 **prefix hit rate가 llm-d 93.5%, FluidServe 75.1%, Llumnix SLO 79.9%**.
  실제로 계산하는 prefill 토큰이 4,585 대 12,274 tok/s(2.7분의 1)이고, 그 시간이 decode로
  가서 20,661 대 16,562 tok/s(+25%)가 된다. KV 점유도 22.2% 대 35.9%.
- **예열 주행 때문이 아니다**: cold restart 직후 예열 주행의 첫 1분이 이미 96.2%다.
- 배관: llm-d는 Llumnix 스케줄러를 안 거치므로 엔진 귀속을 **Envoy access log의
  `%UPSTREAM_HOST%`**에서 얻는다. `analysis_scripts/request_level/llmd_engine_map.py`,
  여덟 조건 전부 99.88~99.97% 매칭.

**예측기가 무엇을 예측하는가 (§9.12, 소스 확인)**: 후보 엔드포인트마다 **새 요청 하나의
(TTFT, TPOT)**만 낸다. **각 요청이 얼마나 더 돌지는 예측하지 않고**(스케줄링 시점 호출이
`generatedTokenCounts=1` 고정), **기존 요청들의 TTFT·TPOT도 예측하지 않는다**(기존 요청은
집계 상태 특징과 `podMinTPOTSLO`라는 **문턱값**으로만 들어온다). 정확도도 특별하지 않다
— p90 목표 대비 coverage 88.1%/89.9%, 예측/실측 1.20배/1.13배로 보수적이다.

**다음에 할 것**: ① rep 2 채점 후 두 반복을 합쳐 편차와 함께 표를 다시 만든다
② swe 격차가 예산 해석 때문인지 가르려면 **FluidServe를 m1f로 한 번** 돌린다(우리 정책은
tier 25를 `e2e:30000`으로 재정의하므로 값이 바뀌어도 동작이 같아야 하는데 확인된 적이 없다)
③ **우리 판정 조건 안에 prefix 재사용을 넣으면 얼마를 회수하는가** — §9.11이 만든 새 질문.

**⚠ 새 기준선을 세울 때 반드시**: swe의 SLO 분해가 arm마다 다르다(m1 = 11,800/25,
m1f = 2,500/52). **E2E 예산을 표현 못 하는 정책에는 m1f를 준다.** CLAUDE.md 함정 A에 있다.

**다음**:
1. **EXP-64** 요청 단위 길이 상한 — 설계 완료, **코드 미구현**. `user` 필드로 힌트를 보내
   두 arm이 같은 요청을 낸다. **H3이 가설이 아니라 성립 조건이 됐다** — 두 arm의 클래스별
   유효 인스턴스 수가 0.3 이상 움직이면 이것도 정확도를 못 잰 것이다(§65.9)
2. **EXP-65** 세 클래스 균일 스케일 = 진짜 robustness 재측정. **2026-08-07 사용자 지시로
   보류**(§65.8)
3. **EXP-66** llm-d 기준선 — 계획서 `llmd-baseline.md`. 코드 변경 일곱 군데(전부 측정
   경로)와 단계별 점검 지점이 거기 있다
4. **함대 2대** — motivation §8의 축 2를 시험하는 유일한 실험
5. swe 거절 원인 — 정적 조건에서도 도착률에 따라 단조(76.9 → 42.3 → 23.1)라 8분 조건에서
   조사 가능

**용어 정정 (2026-08-07)**: **"우리는 예측하지 않는다"고 쓰면 안 된다.** 클래스 조건부
분포를 쓰는 것 자체가 과거로 미래를 예측하는 것이다. 다른 것은 예측의 유무가 아니라
**무엇 하나마다 따로 예측하는가**이다 — 우리는 클래스마다 분포 하나를 두고 그 클래스의
모든 요청에 같은 값을 쓰고, Scorpio·AdaGen은 요청 하나하나에 대해 따로 값을 낸다.
EXP-64 설계서와 motivation §8.5를 고쳤다.

**실험 운영 규칙이 바뀌었다**: **실험을 걸면 감시를 같이 건다.**
`/home/nxclab/tools/watch_experiment.sh`, `exp-run` §3, CLAUDE.md 함정 B. EXP-63이 하루에
두 번 조용히 죽어 4.5시간·2.6시간을 잃었다. `state-brief.sh` 훅이 이제 로그별 생사를 보고한다.

**지금 인용하면 안 되는 수치**: `ms_dev/notes/retracted.tsv`가 정본이고
`ms_dev/scripts/check_numbers.py`가 문서 11개에서 찾아 준다.

**정책 상태**: 결정 규칙은 EXP-27 이후 불변. 배포 바이너리 `c2d970ca`.

## §2 어느 문서부터 읽나

| 읽을 것 | 무엇이 있나 |
|---|---|
| [paper-outline.md](paper-outline.md) | **논문 관련 요청이면 여기부터.** 절 구도, **주장마다 뒷받침하는 실험 번호와 반복 횟수**, 그림 목록, **오독을 막으려면 같이 적어야 하는 것 다섯**, **아직 뒷받침이 없는 것 여덟**. ⚠ **§0이 차단 항목**: PolyServe의 `--polyserve-tier-decode-tokens`가 낡아서(dr 3.58배 과소) 지금 표는 프로파일을 고친 우리 대 안 고친 기준선의 비교다 — **EXP-57이 고친 값으로 재측정 중**(2026-08-05 01:34 예약) |
| [motivation.md](motivation.md) | **motivation·problem 관련 요청이면 여기부터. §2 논증의 정본. 2026-08-06에 전면 재구성했다.** 이제 §0.1의 **여섯 단계**다 — 문제 실존 / **엔진이 한계가 아니다** / **손실은 배치와 admission이 만든다** / 엔진 스케줄러로도 안 된다 / 신호가 부족하다 / 격리와 혼합이 **각각 반대쪽 조건을 어긴다**. **가장 큰 변화 둘**: ① 분리를 재는 지표가 **최다 점유율에서 클래스당 유효 인스턴스 수**(`1/Σs²`, inverse Simpson)로 바뀌었다 — 정적 파티션이 **swe 1.00 / chat 1.00 / dr 2.00**으로 자기 배분을 그대로 말한다(옛 지표로는 83.5라 설명 문단이 필요했다). ② **"우리는 중간이다"가 부등식 두 개**가 됐다(§8) — 아래: 클래스는 자기 일에 필요한 만큼 인스턴스를 받아야 하는데 정적 파티션은 chat에 1.02대를 준다(출력 토큰 기준선 2.50); 위: 가장 빡빡한 클래스가 모든 인스턴스에 있으면 안 되는데 완전 혼합 셋 전부 chat 없는 인스턴스-시각이 1.0%다. **핵심 한 줄: PolyServe는 위쪽 조건을 우리보다 훨씬 잘 만족하는데(67.9% 대 4.4~30.9%) 달성률이 절반 이하다 — 분리 자체가 목적이 아니다.** ⚠ **반증됨: 정적 파티션의 배정도 움직인다**(한 시간에 여섯 번). 차이는 "움직이는가"가 아니라 "인스턴스 한 대 단위로 몇 분에 한 번 뛰는가 vs 요청마다 연속으로"다 |
| [motivation.md](motivation.md) **(옛 요약 — 아직 유효한 부분)** | **§10에 논문 §2가 납득시켜야 하는 주장 다섯 개가 번호로 있고 각각의 근거와 그 근거가 우리 시스템 안의 것인지 다른 시스템과의 비교인지 적혀 있다** — 넷은 우리 것이고 1번("클래스를 서버에 고정하는 방식은 잘 안 된다")만 PolyServe에 기댄다. **§9.1에 그 1번을 우리 시스템 안에서 만드는 방법**(선호의 세기를 0~1로 조절, Go 약 10줄), **§9.2에 스케줄러 배치 로그 유실**(무엇을 재는 값인지부터 풀어 씀), **§11에 쓰지 않기로 한 지어낸 말들**.<br>**2026-08-06 기준 PolyServe는 EXP-57의 고친 프로파일 값이다** — 정적 sweep에서 35 req/s에 34.0으로 가장 먼저 무너지고 70에서 24.0으로 셋 중 가장 높다. **"정적 파티션이 열등하다"는 높은 도착률이 아니라 포화점(26.5 req/s, 다른 둘은 36.4·37.1)으로 말한다.** 이전 요약: 사실 **다섯 개**(문제가 실제로 있다 / 클래스마다 자원과 단위가 다르다 / **용량이 배정의 함수다** / 기존 답 둘이 반대 방향으로 틀린다 / **옳은 배정이 1.48배 움직인다**), 그것이 만드는 **요구 조건 넷과 설계로의 연결**, **추가로 만들 기준선 넷**(1순위가 "독립 결합" — 설계서가 논문의 핵심 실험이라고 미리 적어 둔 칸), 비어 있는 것 일곱. 그림별 문서는 `results/aggregate_analysis/motivation/README.md` |
| [llmd-baseline.md](llmd-baseline.md) | **llm-d를 기준선으로 세우는 계획 (정본).** 답하려는 질문 넷, 지금 환경의 실측값, 배포 경로 둘(file-discovery는 거절이 안 되고 InferencePool은 된다), 고쳐야 하는 파일 일곱, **예측기 온라인 학습이 조건마다 초기화되는 위험**, 단계별 점검 지점, 실행 전에 적은 판정 규칙 |
| [fluidserve-v0.1.1.md](fluidserve-v0.1.1.md) | **여기부터 읽는다.** 현재 상태의 자족적 명세 — v0.1에서 **결정 규칙은 한 줄도 안 바뀌었고** 바뀐 것은 길이 프로파일 하나와 계측. 실측 표(정적·한 시간), **v0.1.1이 아닌 것**, 그리고 **미해결 일곱 개를 우선순위로**(1순위가 45 req/s의 두 상태 편차) |
| [fluidserve-v0.1.md](fluidserve-v0.1.md) | 앞 버전. **결정 규칙·용량 모델·상수 아홉 개는 여기가 정본.** v0.1의 자족적 명세 — 결정 규칙, 무엇을 측정하고 무엇을 설정하는가, 남은 상수 9개, 실측 결과, **v0.1이 아닌 것** |
| [fluidserve-implementation.md](fluidserve-implementation.md) | 시간순 경위. §13 v9~v18과 반증된 가정, §15~§22 v19~v22, §21 보류 항목 |
| `Agent_applications/.../experiments/EXP-NN_*.md` 중 번호가 가장 큰 것 | 지금 돌고 있거나 마지막으로 돌린 실험의 설계·판정 규칙 |

---

## §3 implementation.md 절별 요약 (최신이 위)

> ⚠ **아래 PolyServe 값은 EXP-57 이전(길이 프로파일 수정 전)이다.** 정본은 `ms_dev/notes/numbers.tsv`의 `exp57.*.polyserve.*`이고, 낡은 값 목록은 `ms_dev/notes/retracted.tsv`다. 표 전체를 다시 만들기 전까지 이 열은 인용하지 않는다.

| 절 | 무엇이 있나 |
|---|---|
| **§63** | **여기부터 읽는다. 지금 상태의 정본 (2026-08-06).** EXP-59·60·61과 **축의 재해석**. §63.1 정적 클래스 고정 — `fspin-poly`(PolyServe의 배분) 22.2점 낮고 `fspin-demand`(수요 비례)는 45에서 −1.3, 55에서 **+16.4**. §63.2 **정적 조건은 이 질문에 답할 수 없다**(배분이 그 믹스에서 유래해서 조건 내내 옳다). §63.3 한 시간에서도 핀이 +7.1, 그런데 **구간별로 보면 메커니즘이 한 번 나타난다** — chat이 3.46대를 필요로 하는 m2에서만 핀이 −9.4. §63.4 **이 trace도 답할 수 없다**: 네 구간 전부 반올림 배분이 (2,1,1)로 같다. §63.5 **EXP-60 반복 2 대조군 오염** — 드라이버가 한 셸에서 arm을 돌아 `FS_CLASS_PIN`이 상속됐다(§38의 한 겹 바깥). §63.6 **EXP-61: 엔진 큐가 깊어도 QoServe가 도움이 안 된다**(−0.9~−1.1, goodput −3~−15%). **§63.7이 핵심** — 축이 "얼마나 분리됐나"가 아니라 **"배정이 수요에서 얼마나 떨어져 있나"**다. §63.8 **우리가 swe의 62%를 거절한다**(세 세션 재현) |
| **§62** | **여기부터 읽는다. 지금 상태의 정본 (2026-08-06).** motivation 재구성 + 지표 교체 + **코드 변경 둘** + **실험 둘이 도는 중**. ① **EXP-58**(진행 중, 01:53 KST 시작, 약 5시간): 클래스 선호를 켜기/끄기가 아니라 **0~1의 세기 `w`**로 만들었다(`score = w·share + (1−w)·room`, `--fluidserve-affinity-weight`, 배포 `cf74fe98`). **w=1이 지금까지의 동작과 같고 w=0이 `affinity=false`와 같다** — 끝점이 이미 세 번씩 측정된 조건이라 그 자체가 검증이다. ② **EXP-59**(연쇄 대기 중, 약 3시간): `--fluidserve-class-pin`으로 **클래스를 인스턴스에 고정하는 모드**(staging `c2d970ca`, 미배포). arm 둘 — `fspin_poly`(chat 1/dr 2/swe 1, PolyServe가 고른 배분)와 `fspin_demand`(chat 2/dr 1/swe 1, 출력 토큰 비례). **둘을 두는 이유는 "배정이 고정된 것"과 "고른 배정이 틀린 것"이 지금 안 갈려 있어서**다. §62.6에 **검토에서 나와 즉시 고친 다섯 개**(§2.1 굵은 문장이 자기 표와 어긋남, §7.1이 EXP-53 PolyServe 값을 씀, +2.1/−1.8 불일치, 경고를 어긴 제안 문장, **EXP-56 §6.4의 설명이 틀림 — 그 항은 두 arm 모두 이미 꺼져 있었고 애초에 그 값에 작용할 수 없다**), §62.7에 **계측 정정**(chat 없는 인스턴스-시각의 정의) |
| **§60~§61** | **여기부터 읽는다. 지금 상태의 정본.** §60 EXP-56 클래스 선호 ablation — **H1 확인**(45 req/s에서 +12.2/+6.9/+3.4, 평균 +7.5, 문턱 5.0; 55에서는 평균 +4.8로 문턱 아래이나 여섯 쌍 전부 같은 부호), **H3 확인**(편차 8.5 → 0.6). **메커니즘이 작동했다는 직접 증거**: 창별 클래스 집중도가 51.5/61.2/63.3%인데 끄면 27.4/29.4/30.5%(균등 25.0). **H2는 사전 등록이 틀렸다** — 게이트 신호와 균질성 신호를 상호배타로 적었는데 **둘 다 나온다. 같은 원인(분리)의 두 면이기 때문**이고, 기여도를 가르려면 arm이 하나 더 필요하다. **§60.4: 45 req/s의 "두 상태"는 하나의 연속 축이었다** — 점수와 "chat 없는 인스턴스가 존재한 샘플 비율"의 상관이 **+0.953**(45 안에서만; 풀링하면 +0.444). **§60.5는 정정 절이다**: `fsnoaff/full`을 "0행으로 죽었다"고 먼저 썼는데 **292,854행이 `shards/`에 있었고 병합 단계만 안 돈 것**이었다. 복구하니 **한 시간 이득이 +1.8점** — 정적 45 req/s의 +7.5와 크게 다르다. **게이트 산술이 구속력을 갖는 띠가 좁고 한 시간 trace는 띠 밖에서 보내는 시간이 길기 때문**이고, 논문에 정적 값만 쓰면 과대 진술이다. §61은 노드 **DiskPressure**(EXP-57을 러너 Job 뜨기 전에 중단, 오염 없음) → **shard 전부 삭제로 해결**(1.5 TB → 777 GB) → **재발 방지 코드까지 들어감** |
| **§59** | EXP-54 반복 2. **점수가 대단히 잘 재현된다** — offered **FluidServe 69.6±0.3 / Llumnix SLO 39.0±0.2 / PolyServe 17.1±0.5**, goodput **17,967±106 / 11,545±55 / 3,192±7**, 총 처리량은 18,911 / 16,176 / 18,212로 **3.8% 차이인데 goodput 5.6배**. **59.2: 클래스 분리는 두 반복 모두 일어나고(창별 최대 점유율 99~100%) 다른 것은 어느 엔진이냐다** — 반복 1은 60분 내내 엔진 1, 반복 2는 엔진 2 → 3 → 1로 두 번 옮겨 감. **59.3은 정정 절이다**: "분리가 재현되지 않는다"고 먼저 썼는데 **한 시간 전체로 풀링한 집중도가 정체가 움직이는 양을 못 재기 때문**이었다(§40·§55·§32와 같은 계열). **그림이 표를 먼저 반증했다.** 새 규칙: **집중되는 대상의 정체가 움직일 수 있으면 전 구간 풀링으로 집중도를 재지 않는다.** §58.3의 인과 주장은 **확인도 반증도 안 된 상태**이고 재려면 `--fluidserve-enable-affinity=false` ablation이 필요하다. **59.5: preemption은 fleet 총계로는 재현되고(2,965 대 2,922) 엔진별 배분은 완전히 달라진다.** **59.7**: `classharm=false` 모순은 **풀렸다 — 드라이버가 모든 FluidServe arm에 고정한 것이고 그 줄 위에 이유가 적혀 있다**(비교 대상이 그렇게 돌았으므로). 양방향으로 확인했고 플래그 경로도 기동 줄도 정상. **놓친 것은 값을 설정하는 곳을 안 본 것.** migration 0건은 그대로 남음 |
| **§58** | 그 앞. EXP-54 반복 1. §58.1 총 생산량 4% 차이에 goodput 5.7배. **§58.3의 관측은 유효하나**(반복 1은 dr이 60분 내내 엔진 8001) **인과 주장은 확인도 반증도 안 된 상태**(§59.3). **58.4: Llumnix 기본 arm 중단** — 거절하지 않으니 40분에 포화되고 **부하 생성기가 임시 포트를 고갈**(Errno 99 62,114건, 다른 arm 0건), 클라이언트 수정 전에는 못 잰다. PolyServe는 chat 엔진 둘이 **96.4·101.0ms**에 큐 445·1,121, 엔진 불균형 **48배**(FluidServe 4.5배) |
| **§57** | 그 앞. EXP-53 네 정책 정적 sweep(2반복 64조건). offered 45 req/s에서 **FluidServe 90.2 / Llumnix SLO 52.4 / Llumnix 32.1 / PolyServe 27.5**, 70에서 **51.9 / 21.4 / 3.9 / 14.4**, goodput 70에서 **19,435 / 11,737 / 802 / 3,965**. **총 생산량은 37% 차이인데 goodput은 24배** — 프로젝트 출발 주장이 정책 비교로 재현됨. **확인 필요 둘**: migration arm 구분이 주장대로가 아닐 수 있음(스케줄러 재배치 루프가 엔진 설정과 무관하게 돎), Llumnix SLO의 admitted 상승은 chat만 88%까지 거절해 분모가 바뀐 것 |
| **§51~§56** | 그 앞. §51 후보 C 재측정(정적 +10.6·preemption −33%인데 동적 −4.4로 기각). §52 45 req/s **쌍안정**과 상태 변수(chat 없는 엔진 유무, 24대24 예외 없음). §53 C가 붕괴를 없앰(0/8 대 8/20)—affinity가 feasible 안에서만 도니 게이트가 닫히면 분리가 안 생긴다. §54 게이트를 **축(gateSlack)** 으로. §55 `c_kv` 1/1.6 주장 정정(실제 1/1.26). §56 축은 단조롭지 않고 양 끝으로 붕괴<br>**§51의 상세(EXP-50)** — 후보 C를 수정 프로파일 위에서 다시. **정적 +10.6점**(60 req/s에서 58.6 → 69.2, 편차 0.1, 거절률 하락, goodput +15%, dr 84.7→100.0, swe 22.0→82.9). **EXP-46의 기각 사유였던 preemption 6,583은 철회된다** — 올바른 입력 위에서 C는 2,794 → **1,873으로 33% 줄인다.** **새 기각 사유는 chat이다**: 한 시간에서 offered 69.7 → 65.3, 거절 27.0 → 30.7, chat **−14.4**(요청의 76.9%), dr +16.9, swe +48.0. **게이트는 두 일을 겸하고 있었다 — 서빙 가능한 dr을 거절하는 일과 가장 빡빡한 클래스를 위해 용량을 남기는 일. C는 둘 다 없앤다.** 다음은 §51.5의 둘(후보 B, 게이트에 클래스별 하한) |
| **§50** | 그 앞의 정본. EXP-49 — **H2 기각**(preemption 2,724 → 1,748로 36% 줄지만 offered 2.4점·goodput 3.4% 손해, 규칙은 500 미만을 요구). **투영 자체는 고쳐졌다**(배포된 투영 평균오차 −26,841 → +13,341, MAE −28%) — 그런데 점수가 나빠졌다. 그리고 계수기가 처음으로 **무엇이 배치를 막는지**를 세었다: **페이스 게이트 90.8%, memory 7.2%**. **§47.3의 "재고 조건으로 유량을 통제한다"는 진단은 구속력 없는 항을 겨냥하고 있었다.** 게이트를 바꾸는 유일한 제안이 **후보 C**이고 EXP-50이 수정 프로파일 위에서 다시 잰다 |
| **§49** | 그 앞의 정본. EXP-48 결과 — **길이 프로파일 수정이 지금까지의 어떤 정책 변경보다 크다.** 정적 60 req/s에서 offered가 네 번 재서 36.6이던 것이 **56.8·56.9**(편차 0.1), goodput 12,466 → 18,694. 한 시간 trace에서 59.7 → **70.2**(편차 1.1), admitted **95.7**(이 trace 최고), 거절률은 오히려 하락. **§36·§44가 하루 들여 분해했던 50~56분 손실이 16.5 → 35.9로 Llumnix SLO의 17.3을 처음 넘는다.** Llumnix SLO 대비 +21.2 → **+31.9점**. 그런데 **preemption이 1,471~1,852 → 2,776으로 올랐고**, 사전 판정 규칙("1,400 초과면 도착 항 결여가 주원인")대로 **H2가 다음이다**(EXP-49 진행 중) |
| **§48** | 그 앞의 정본. 투영을 처음으로 실제 미래와 대조했다 — 배포된 `proj`가 **88.5%의 경우 과소예측**(평균 −101,633 토큰, MAE 106k)이고 **투영을 안 하는 것(MAE 29k)보다 나쁘다.** 편향은 엔진이 찰수록 커진다(−7.1% → −22.7%). 원인 둘: ① **deepresearch 길이 프로파일이 3.4배 짧았다**(282 대 985) — `5fa82f8`이 07-29에 워크로드를 바꿨는데 프로파일을 안 고쳤다. **고쳤고 EXP-48이 재고 있다.** ② **흐름 수지에 도착 항이 없다**(horizon당 약 48건, 약 83,000 토큰). **후보 H는 §48.1에서 철회**, 다음은 **H2**(관측 KV 변화율) |
| **§44~§47** | 그 앞의 정본. §44 50~56분 격차는 전부 deepresearch. §45 **"후보 A가 preemption을 0으로 유지한다"는 철회** — 0/0/1,899. §46 EXP-47: **한 상태 읽기 안에서 뷰는 갱신된다**(두 번째 이후 배치의 headroom이 평균 −57k) → in-flight 가설 반증. §47 **preemption은 FORCE가 아니라 엔진이 채워지는 동안** 일어난다(arm 셋·엔진 다섯에서 예외 없음) |
| **§39~§43** | §39 **후보 A 채택 — EXP-27 이후 첫 정책 변경**(60 req/s에서 offered 36.1→49.3, goodput +33%). §40 §36.3의 원인 설명 **철회**(평균끼리 나눈 값이었다). §41 한 갱신 주기에 여러 건이 나간다는 측정. §42 §41.4 **정정**(in-flight 계정은 설계상 그 창을 덮는다). §43 EXP-43 null + **후보 여섯 개의 현황표(43.4)** |
| **§37** | **다음에 무엇을 고칠지가 여기 있다.** 세 실패(높은 rate에서의 하락 / 8003 과부하 / 후반부 역전)의 원인 규명. 셋이 만나는 곳은 **route 결정 비율**(15 req/s에서 99.7% → 60에서 1.5%). §37.5는 ROUTE와 FORCE가 같은 예산에 다른 기준을 쓴다는 코드 불일치. **§37.6에 후보 넷과 판정 규칙이 실행 전에 적혀 있다** |
| **§36** | **EXP-41(한 시간 동적 trace 두 개).** 두 trace 모두 +21.2/+26.4점. **클래스 packing이 작동한다는 첫 직접 증거**(azcode에서 chat 전용 엔진 두 개, chat ITL 26.6~32.6ms, chat 45.3→85.4)와 **그 대가**(dr 전용 엔진이 preemption 5,513회). 설계 결함 둘의 원인이 36.3(`capMem`에 전방 모델 없음)·36.4(포화에서 게이트 50 고정)이고 **36.3은 판정 규칙까지 미리 적어 뒀다.** 36.5는 36.3의 원인을 처음에 잘못 귀속한 것의 정정 |
| **§35** | EXP-40 — 우위가 엔진 때문인지. **아니다**(격차 변화가 편차 안). 사전 예상이 틀렸고 그 이유가 §33.1을 뒷받침한다 |
| **§34** | 60 req/s에서 무엇이 무너지는가. 배치는 45와 같고 페이스만 4.5ms 올라 chat 예산을 넘는다. **1ms = 총계 7.7점의 절벽.** 34.4는 배포된 `c_kv`가 실측의 1/1.6이라는 부수 발견 |
| **§33** | 설계 검토 — 포화에서 정책이 하는 일, `gate_allowance` 50ms 고정, agent 클래스 실패의 원인 |
| **§32** | **기록된 TBT가 실제의 1/1.92였다 — §1~§31의 모든 attainment가 예산 약 2배로 판정된 것이다.** 세 방향 검증과 반증 시도는 §32.2·§32.7 |
| **§31** | 그 이전의 상태 요약(정책 불변, 반증된 가정 여섯 개). **§31.7의 다음 계획은 §32와 EXP-39가 대체했다** |
| **마지막 절** | 그 이후에 일어난 일. 항상 문서 끝이 가장 최신이다 |
