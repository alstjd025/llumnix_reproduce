# 2026-08-28 점검(홈 전체 초기화) 뒤 환경 복원 절차

> **이 절차는 2026-08-31에 실제로 실행됐고, 그때 발견한 것을 반영해 고쳤다.**
> 고친 곳 넷: §1 표 둘(`.gitconfig` 미전송, `results_priority` 실제 크기),
> **§2.1 신설**(git이 나르지 않아 백업에서만 오는 것 — 이 중 셋이 없으면 빌드나
> 러너가 그 자리에서 멈춘다), §4의 경고(**커밋된 배포 YAML이 실제 구성보다 낡다**),
> §5.2의 기대값 정정(`enableforce`는 `false`가 아니라 `true`).

2026-08-28 18:00 KST부터 3일 점검으로 **`/home/nxclab` 전체가 초기화된다.**
살아남는 것은 ⑴ 외부 스토리지 `/NHNHOME/NXC_ROOT/NXC13/home_backup_2026-08-28/`
(이 날 만든 전체 백업, 아래 §1)과 ⑵ GitHub의 두 저장소다. 이 문서는 백업 루트와
저장소 양쪽에 있다.

## 0. 재개 첫 날의 순서 (요약)

1. 백업에서 홈 복원 (§2) — 저장소·도구·dotfiles·메모리까지 한 번에.
2. k3s 클러스터 재구축 (§3) — 기존 셋업 기록 그대로.
3. 배포 상태 복원 (§4) — 스케줄러 `08f9e9ce`, 엔진 stock, migration off.
4. 검증 run 하나 (§5) — 복원된 환경이 점검 전과 같은 값을 내는지.
5. 그 다음에야 개발 재개: **첫 작업은 v0.4 기본값 이동**
   (STATUS.md §1과 `experiments/EXP-107_class-instance-cap.md` §9).

## 1. 백업에 무엇이 있나 (`home_backup_2026-08-28/`)

| 경로 | 내용 |
|---|---|
| `llumnix_reproduce/` | 저장소 전체(.git 포함, **results/ 제외**). `Agent_applications/`(자체 .git), `lib/sglang`(게이트웨이 Rust 바인딩 — git 미추적이라 이 사본이 유일), `bin/`(배포 바이너리), workload 입력(transcript 셋), traces |
| `tools/` | go 툴체인, `bin-backup/`(바이너리 사슬), staging, watch/체인 스크립트, 실험 로그 |
| `dotfiles/` | `.claude/`(**세션 메모리 포함**), `.kube/`, `.bashrc`, crontab. ⚠ `.gitconfig`는 여기 적혀 있었으나 **실제로는 전송되지 않았다**(2026-08-31 확인) |
| `cluster_state/` | 점검 직전 클러스터의 실제 상태 덤프(all/configmaps/lws/nodes yaml) — 재구축 시 대조 기준 |
| `results_priority/` | `paper_experiment/`(논문 고정 세트), `results/aggregate_analysis/`(그림+빌드 스크립트), EXP-104·105·107 run 전부, EXP-93 llm-d 대조 run 둘. ⚠ **실제 크기는 매니페스트의 151 GB가 아니라 약 417 GB다** — 그 숫자는 15:32에 센 것이고 §6의 2차 스트림이 18:00까지 계속 들어갔다. 2026-08-31 복원에서 `results/`만 **414.7 GB**를 옮겼다(rc=0, 약 1시간 45분) |
| (여유가 되면) `results_rest/`, `hf-cache/` | §6의 2차분 — 전송이 끝났는지는 백업 루트의 `BACKUP_MANIFEST.txt`로 확인 |

**백업에 없는 것**: `results/`의 나머지(주로 `tbt_events.jsonl` — 지표는 전부
`metrics.csv`에서 유도되고, 논문이 참조하는 run은 `results_priority/`에 있다),
`gotmp/`. 이들이 없어서 재현 불가능해지는 결론은 없다(고정 세트가 기준).

## 2. 홈 복원

**저장소는 백업 사본이 아니라 `git fetch`로 맞춘다.** 백업의 두 저장소 사본은
2026-08-28 12:51 것이라 그날 오후 커밋이 들어 있지 않다(`BACKUP_MANIFEST` §2).
그래서 복원은 **추적되는 파일은 git, 추적되지 않는 파일은 백업**으로 갈라서 한다.
아래 §2.1이 그 갈라진 목록이고, 통째로 덮어쓰는 아래 명령은 저장소를 clone하지
않은 경우에만 쓴다.

```bash
B=/NHNHOME/NXC_ROOT/NXC13/home_backup_2026-08-28
rsync -a "$B/llumnix_reproduce" /home/nxclab/
rsync -a "$B/tools" /home/nxclab/
rsync -a "$B/dotfiles/.claude" "$B/dotfiles/.kube" /home/nxclab/
cp -p "$B/dotfiles/.bashrc" "$B/dotfiles/.gitconfig" /home/nxclab/
mkdir -p /home/nxclab/tools/gotmp
# 결과 데이터
mkdir -p /home/nxclab/llumnix_reproduce/Agent_applications/agent_motivation_experiment/results
rsync -a "$B/results_priority/paper_experiment" \
  /home/nxclab/llumnix_reproduce/Agent_applications/agent_motivation_experiment/
rsync -a "$B/results_priority/results/" \
  /home/nxclab/llumnix_reproduce/Agent_applications/agent_motivation_experiment/results/
rsync -a "$B/results_priority/aggregate_analysis" \
  /home/nxclab/llumnix_reproduce/Agent_applications/agent_motivation_experiment/results/
```

### 2.1 git이 나르지 않아 백업에서만 오는 것 (2026-08-31 복원에서 확인)

`.gitignore` 대상이거나 서브모듈이라 `git clone` + `git fetch`로는 오지 않는다.
아래 넷은 없으면 **빌드나 실험이 그 자리에서 멈춘다.**

| 경로 | 없으면 무슨 일이 나나 |
|---|---|
| `Agent_applications/agent_motivation_experiment/.venv` (658 MB) | 러너 Job이 첫 줄에서 죽는다 — `/work/.venv/bin/python: No such file or directory`, 이어서 `[runner] venv missing deps`. 파드는 이 venv의 인터프리터를 쓰고(심볼릭 링크가 이미지에도 있는 `/usr/bin/python3.12`로 풀린다) pip을 안 쓴다 |
| 생성된 protobuf 파일 넷 — `pkg/cms/cms.pb.go`, `pkg/resolver/redis_discovery.pb.go`, `pkg/scheduler/llumlet/llumlet_server{,_grpc}.pb.go` | `go build`·`go test`가 `undefined: InstanceStatus` / `undefined: PodDiscoveryInfo`로 실패한다. 저장소에 코드 생성 단계가 없으므로 이 사본이 유일본이다 |
| `.git/modules/lib/sglang` (52 MB) | `lib/sglang`은 서브모듈이고 그 작업트리 안의 `.git`은 `../../.git/modules/lib/sglang`을 가리키는 포인터다. 모듈 디렉토리가 없으면 **저장소 루트에서 모든 git 명령이** `fatal: not a git repository: lib/sglang/../../.git/modules/lib/sglang`으로 죽는다. 백업 복사에서 `.git/`을 제외하면 정확히 이 상태가 된다 |
| `patches/vllm-sched/VLLM_VERSION.txt` | 엔진에 read-only로 마운트되는 디렉토리의 일부다. `SCHED_EXTRA_ARGS`가 비어 있으면 읽히지 않지만, 엔진 스케줄러를 켜는 조건에서는 필요하다 |

그 밖에 백업에만 있고 없어도 실험은 도는 것: `lib/sglang` 작업트리(78 MB, 게이트웨이
빌드에 필요), `bin/`(배포 바이너리 둘), `traces/`(1.8 GB), swe transcript 둘,
`JITServe/`·`sarathi-serve-niyama/`(관련 연구 분석용 클론 106 MB),
`.claude/skills/paper-writing/`, `ms_dev/fluidserve.txt`.

```bash
B=/NHNHOME/NXC_ROOT/NXC13/home_backup_2026-08-28/llumnix_reproduce
R=/home/nxclab/llumnix_reproduce
rsync -a "$B/lib/sglang/" "$R/lib/sglang/"
mkdir -p "$R/.git/modules/lib" && rsync -a "$B/.git/modules/lib/sglang" "$R/.git/modules/lib/"
rsync -a "$B/bin/" "$R/bin/"
for f in pkg/cms/cms.pb.go pkg/resolver/redis_discovery.pb.go \
         pkg/scheduler/llumlet/llumlet_server.pb.go \
         pkg/scheduler/llumlet/llumlet_server_grpc.pb.go \
         patches/vllm-sched/VLLM_VERSION.txt; do cp -p "$B/$f" "$R/$f"; done
A=$R/Agent_applications/agent_motivation_experiment
rsync -a "$B/Agent_applications/agent_motivation_experiment/.venv" "$A/"
rsync -a "$B/Agent_applications/agent_motivation_experiment/traces/" "$A/traces/"
rsync -a "$B/Agent_applications/agent_motivation_experiment/workloads/codingagent_request_level_poisson/data/" \
         "$A/workloads/codingagent_request_level_poisson/data/"
rsync -a "$B/JITServe" "$B/sarathi-serve-niyama" "$R/"
```

`.gitconfig`는 **백업에 없다**(§1 표가 있다고 적었으나 실제로는 전송되지 않았다).
커밋 author에서 복원한다: `alstjd025 <alstjd025@gmail.com>`.

git 원격이 살아 있으므로 `git fetch`로 백업 시점 이후를 대조할 수 있다.
⚠ 실험 저장소는 2026-08-28에 **최근 커밋들이 재작성**됐다(100 MB 초과 transcript
둘 제거) — 옛 해시(33e8ca0 등)를 어디서 보더라도 새 해시(c9f2ad8 계열)가 정본이다.
transcript 세 파일의 md5 대조 기준은
`workloads/codingagent_request_level_poisson/data/README.md`.

## 3. k3s 클러스터 재구축

**기록이 이미 있다 — 그대로 따른다** (전부 복원된 저장소 안):
- k3s 설치: [`nxc7-k3s-install-recipe.md`](nxc7-k3s-install-recipe.md)
  (NXC13도 같은 절차로 세웠다), 셋업 전반: [`llumnix-k3s-setup-notes.md`](llumnix-k3s-setup-notes.md)
- 배포 형태: [`llumnix-neutral-deploy-working.md`](llumnix-neutral-deploy-working.md),
  [`llumnix-deploy-modes.md`](llumnix-deploy-modes.md)
- 매니페스트: `deploy/neutral/`(엔진 lws), `deploy/profiling/`(프로파일 ConfigMap 원본),
  러너는 `Agent_applications/.../k8s/`(runner-rbac, runner-dyn.template)
- **점검 직전 실물 대조**: 백업의 `cluster_state/*.yaml` — 특히 `lws_neutral.yaml`의
  env(`LLUMNIX_ENABLE_MIGRATION=0`, `SCHED_EXTRA_ARGS` 빈 값), 게이트웨이 deploy의
  `GOMAXPROCS=16`과 큐 설정(`--wait-queue-threads 4096 --max-queue-size 16384`),
  스케줄러 args 전문

핵심 hostPath 계약(러너·스케줄러가 가정): `bin/` → 스케줄러 파드 `/exp07bin`,
`Agent_applications/agent_motivation_experiment` → 러너 `/work`, 프로파일 ConfigMap →
`/profiling`. 엔진 이미지는
`llumnix-registry.cn-beijing.cr.aliyuncs.com/llumnix/vllm:20260306-165123`.

모델 가중치(HF 캐시가 백업에 없으면):
`meta-llama/Meta-Llama-3.1-70B-Instruct`, snapshot
`1605565b47bb9346c5515c34102e054115b4f98b` — hf-cache hostPath에 다시 받는다.

## 4. 배포 상태 (점검 직전 기준)

| | 값 |
|---|---|
| 스케줄러 | `bin/scheduler-exp07` = **`6dc9f035`** (EXP-107T 판 — guardrail cap + force 플래그 + `tier:decode:<ms>` 예산 문법). 파드에서 `md5sum /proc/1/exe`로 확인 |
| 바이너리 사슬 | `tools/bin-backup/scheduler-exp07.pre-exp107t`=`08f9e9ce`(guardrail 판), `.pre-exp107g`=`092edd73`(구판 cap), `.pre-exp107`=`d867351a`(v0.3), 그 앞은 기존 사슬 |
| swe SLO | **per-token 형태로 전환 결정됨(2026-08-28)**: TTFT 7초 + 75 ms/token. 정책은 `--fluidserve-class-budgets "25:decode:75,…"`(FS_SWE_TBT_MS=75), TTFT는 `workload_configs/mix_dyn60_shift_m2Am1B_b1045_t75.json`. 채점은 같은 규칙으로(arm 이름 `t75`) — **e2e 채점 표와 절대 혼합 금지.** 근거는 EXP-107 §10.2(약속=수명 적분 대 집행=순간의 불일치 측정) |
| 게이트웨이 | `bin/gateway-exp10` (2026-07-27 빌드, `lib/sglang` 바인딩 필요 — ldflags는 CLAUDE.md 빌드 절) |
| 기본값 | cap **꺼짐** / force **켜짐** (컴파일 기본). 채택 구성은 arm이 플래그로 켠다 — **v0.4 기본값 이동이 재개 후 첫 작업** |
| 엔진 | stock ordering, migration off |

⚠ **커밋된 배포 YAML은 여기 적힌 구성이 아니다 (2026-08-31 확인).** `deploy/neutral/
full-mode-scheduling/load-balance/`의 세 파일을 그대로 배포하면 **모델이
`Meta-Llama-3-70B-Instruct`, 컨텍스트 8192, migration 켜짐, 스케줄러·게이트웨이가
스톡 이미지 안의 바이너리**로 뜬다. 실제로 돌던 구성은 그 값들이 클러스터에
직접 적용된 뒤 파일로 돌아오지 않은 것이고, 백업 작업트리의 YAML도 git HEAD와
같았다. 2026-08-31에 `cluster_state/`를 기준으로 세 파일을 맞췄다:

- `neutral.yaml` — 모델 `Meta-Llama-3.1-70B-Instruct`, `--max-model-len 40960`,
  `${SCHED_EXTRA_ARGS}` 자리, `LLUMNIX_ENABLE_MIGRATION=0`,
  `PYTHONPATH`/`SCHED_EXTRA_ARGS`/`SCHED_STEP_LOG`, `patches/vllm-sched` 마운트 둘
- `scheduler.yaml` — `command: /exp07bin/scheduler-exp07`, `bin/` hostPath
- `gateway.yaml` — `command: /exp07bin/gateway-exp10`, 토크나이저 3.1 스냅샷,
  `--max-model-len 40960`, `GOMAXPROCS=16`과 SSE 타임아웃, 메모리 한도 64Gi, `bin/` hostPath

맞춘 뒤 남는 차이는 전부 `set_scheduler_profiling.py`가 조건마다 설정하는 플래그다
(정책·SLO·FluidServe 계열·게이트웨이 큐). 프로파일 ConfigMap 볼륨도 그 스크립트가 넣는다.

## 5. 복원 검증

1. 단위: `go test ./pkg/scheduler/policy/` 전부 통과.
2. 파이프라인: `run_exp107_capforce.sh smoke` (`SMOKE_ARM=fsv3capg`) — 기동 줄에
   `instancecap=true, capmult=3.0, enableforce=true`, `verified:` 통과,
   `instcap_*` 시리즈가 scheduler.jsonl에 저장.
   ⚠ **이 줄은 처음에 `enableforce=false`라고 적혀 있었고 틀렸다 (2026-08-31 정정).**
   `fsv3capg`는 `FS_FORCE=true`를 고정하고, force를 끈 arm은 `fsv3capgnofrc`다.
   채택 구성(cap on + force off)과 smoke가 쓰는 arm을 섞어 적은 것이고, 그대로
   대조했으면 **정상 복원을 실패로 읽었을** 자리다.
3. 값: 짧은 trace의 fsv3 smoke 수치가 백업된 `260827_0507_exp107asmoke` /
   `260827_1424_exp107gsmoke`와 자릿수에서 일치.
4. 세션 규칙대로, 본 실험 전에 대조 arm 하나를 재측정해 기존 값과 대조한다.

**2026-08-31 실측 (같은 arm `fsv3capg`, 같은 9분 ablation trace):**

| | 점검 전 `260827_1424` | 복원 후 `260830_2240` |
|---|---|---|
| 전달된 도착률 | 40.1/s | 40.1/s |
| 요청 수 | 20,937 | 20,989 |
| offered 달성률 | 80.8 | 79.4 |
| admitted 달성률 | 97.8 | 97.5 |
| 거절률 | 17.1% | 18.2% |
| token goodput | 15,052/s | 14,708/s |
| 엔진 | 4/4 | 4/4 |

offered 차이 1.4점은 이 워크로드의 반복 간 편차(세션 안 1.4~3.3점, 세션 간 4.6점)
아래이므로 **읽을 수 있는 차이가 없다**. 양쪽 다 반복 1회이므로 "같다"가 아니라
그렇게 적는다.

## 6. 2차 백업분 (시간이 허락한 만큼; `BACKUP_MANIFEST.txt`가 최종 목록)

우선순위: ⑴ `results/`의 나머지 run (오래된 것부터가 아니라 **정본 문서가 참조하는
것부터** — excluded_runs·manifest 참조), ⑵ hf-cache. 점검 전 마지막 rsync pass가
EXP-107I 결과와 최종 커밋을 덮어썼는지 `BACKUP_MANIFEST.txt`의 시각으로 확인.
