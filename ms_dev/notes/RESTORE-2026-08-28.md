# 2026-08-28 점검(홈 전체 초기화) 뒤 환경 복원 절차

2026-08-28 18:00 KST부터 3일 점검으로 **`/home/nxclab` 전체가 초기화된다.**
살아남는 것은 ⑴ 외부 스토리지 `/NHNHOME/NXC_ROOT/NXC13/home_backup_2026-08-28/`
(이 날 만든 전체 백업, 아래 §1)과 ⑵ GitHub의 두 저장소다. 이 문서는 백업 루트와
저장소 양쪽에 있다.

## 0. 재개 첫 날의 순서 (요약)

1. 백업에서 홈 복원 (§2) — 저장소·도구·dotfiles·메모리까지 한 번에.
2. k3s 클러스터 재구축 (§3) — 기존 셋업 기록 그대로.
3. 배포 상태 복원 (§4) — 스케줄러 `08f9e9ce`, 엔진 stock, migration off.
4. 검증 run 하나 (§5) — 복원된 환경이 점검 전과 같은 값을 내는지.
5. 그 다음에야 개발 재개: **첫 작업은 v0.4 기본값 이동**
   (STATUS.md §1과 `experiments/EXP-107_class-instance-cap.md` §9).

## 1. 백업에 무엇이 있나 (`home_backup_2026-08-28/`)

| 경로 | 내용 |
|---|---|
| `llumnix_reproduce/` | 저장소 전체(.git 포함, **results/ 제외**). `Agent_applications/`(자체 .git), `lib/sglang`(게이트웨이 Rust 바인딩 — git 미추적이라 이 사본이 유일), `bin/`(배포 바이너리), workload 입력(transcript 셋), traces |
| `tools/` | go 툴체인, `bin-backup/`(바이너리 사슬), staging, watch/체인 스크립트, 실험 로그 |
| `dotfiles/` | `.claude/`(**세션 메모리 포함**), `.kube/`, `.bashrc`, `.gitconfig`, crontab |
| `cluster_state/` | 점검 직전 클러스터의 실제 상태 덤프(all/configmaps/lws/nodes yaml) — 재구축 시 대조 기준 |
| `results_priority/` | `paper_experiment/`(논문 고정 세트), `results/aggregate_analysis/`(그림+빌드 스크립트), EXP-104·105·107 run 전부, EXP-93 llm-d 대조 run 둘 |
| (여유가 되면) `results_rest/`, `hf-cache/` | §6의 2차분 — 전송이 끝났는지는 백업 루트의 `BACKUP_MANIFEST.txt`로 확인 |

**백업에 없는 것**: `results/`의 나머지(주로 `tbt_events.jsonl` — 지표는 전부
`metrics.csv`에서 유도되고, 논문이 참조하는 run은 `results_priority/`에 있다),
`gotmp/`. 이들이 없어서 재현 불가능해지는 결론은 없다(고정 세트가 기준).

## 2. 홈 복원

```bash
B=/NHNHOME/NXC_ROOT/NXC13/home_backup_2026-08-28
rsync -a "$B/llumnix_reproduce" /home/nxclab/
rsync -a "$B/tools" /home/nxclab/
rsync -a "$B/dotfiles/.claude" "$B/dotfiles/.kube" /home/nxclab/
cp -p "$B/dotfiles/.bashrc" "$B/dotfiles/.gitconfig" /home/nxclab/
mkdir -p /home/nxclab/tools/gotmp
# 결과 데이터
mkdir -p /home/nxclab/llumnix_reproduce/Agent_applications/agent_motivation_experiment/results
rsync -a "$B/results_priority/paper_experiment" \
  /home/nxclab/llumnix_reproduce/Agent_applications/agent_motivation_experiment/
rsync -a "$B/results_priority/results/" \
  /home/nxclab/llumnix_reproduce/Agent_applications/agent_motivation_experiment/results/
rsync -a "$B/results_priority/aggregate_analysis" \
  /home/nxclab/llumnix_reproduce/Agent_applications/agent_motivation_experiment/results/
```

git 원격이 살아 있으므로 `git fetch`로 백업 시점 이후를 대조할 수 있다.
⚠ 실험 저장소는 2026-08-28에 **최근 커밋들이 재작성**됐다(100 MB 초과 transcript
둘 제거) — 옛 해시(33e8ca0 등)를 어디서 보더라도 새 해시(c9f2ad8 계열)가 정본이다.
transcript 세 파일의 md5 대조 기준은
`workloads/codingagent_request_level_poisson/data/README.md`.

## 3. k3s 클러스터 재구축

**기록이 이미 있다 — 그대로 따른다** (전부 복원된 저장소 안):
- k3s 설치: [`nxc7-k3s-install-recipe.md`](nxc7-k3s-install-recipe.md)
  (NXC13도 같은 절차로 세웠다), 셋업 전반: [`llumnix-k3s-setup-notes.md`](llumnix-k3s-setup-notes.md)
- 배포 형태: [`llumnix-neutral-deploy-working.md`](llumnix-neutral-deploy-working.md),
  [`llumnix-deploy-modes.md`](llumnix-deploy-modes.md)
- 매니페스트: `deploy/neutral/`(엔진 lws), `deploy/profiling/`(프로파일 ConfigMap 원본),
  러너는 `Agent_applications/.../k8s/`(runner-rbac, runner-dyn.template)
- **점검 직전 실물 대조**: 백업의 `cluster_state/*.yaml` — 특히 `lws_neutral.yaml`의
  env(`LLUMNIX_ENABLE_MIGRATION=0`, `SCHED_EXTRA_ARGS` 빈 값), 게이트웨이 deploy의
  `GOMAXPROCS=16`과 큐 설정(`--wait-queue-threads 4096 --max-queue-size 16384`),
  스케줄러 args 전문

핵심 hostPath 계약(러너·스케줄러가 가정): `bin/` → 스케줄러 파드 `/exp07bin`,
`Agent_applications/agent_motivation_experiment` → 러너 `/work`, 프로파일 ConfigMap →
`/profiling`. 엔진 이미지는
`llumnix-registry.cn-beijing.cr.aliyuncs.com/llumnix/vllm:20260306-165123`.

모델 가중치(HF 캐시가 백업에 없으면):
`meta-llama/Meta-Llama-3.1-70B-Instruct`, snapshot
`1605565b47bb9346c5515c34102e054115b4f98b` — hf-cache hostPath에 다시 받는다.

## 4. 배포 상태 (점검 직전 기준)

| | 값 |
|---|---|
| 스케줄러 | `bin/scheduler-exp07` = **`6dc9f035`** (EXP-107T 판 — guardrail cap + force 플래그 + `tier:decode:<ms>` 예산 문법). 파드에서 `md5sum /proc/1/exe`로 확인 |
| 바이너리 사슬 | `tools/bin-backup/scheduler-exp07.pre-exp107t`=`08f9e9ce`(guardrail 판), `.pre-exp107g`=`092edd73`(구판 cap), `.pre-exp107`=`d867351a`(v0.3), 그 앞은 기존 사슬 |
| swe SLO | **per-token 형태로 전환 결정됨(2026-08-28)**: TTFT 7초 + 75 ms/token. 정책은 `--fluidserve-class-budgets "25:decode:75,…"`(FS_SWE_TBT_MS=75), TTFT는 `workload_configs/mix_dyn60_shift_m2Am1B_b1045_t75.json`. 채점은 같은 규칙으로(arm 이름 `t75`) — **e2e 채점 표와 절대 혼합 금지.** 근거는 EXP-107 §10.2(약속=수명 적분 대 집행=순간의 불일치 측정) |
| 게이트웨이 | `bin/gateway-exp10` (2026-07-27 빌드, `lib/sglang` 바인딩 필요 — ldflags는 CLAUDE.md 빌드 절) |
| 기본값 | cap **꺼짐** / force **켜짐** (컴파일 기본). 채택 구성은 arm이 플래그로 켠다 — **v0.4 기본값 이동이 재개 후 첫 작업** |
| 엔진 | stock ordering, migration off |

## 5. 복원 검증

1. 단위: `go test ./pkg/scheduler/policy/` 전부 통과.
2. 파이프라인: `run_exp107_capforce.sh smoke` (`SMOKE_ARM=fsv3capg`) — 기동 줄에
   `instancecap=true, capmult=3.0, enableforce=false`, `verified:` 통과,
   `instcap_*` 시리즈가 scheduler.jsonl에 저장.
3. 값: 짧은 trace의 fsv3 smoke 수치가 백업된 `260827_0507_exp107asmoke` /
   `260827_1424_exp107gsmoke`와 자릿수에서 일치.
4. 세션 규칙대로, 본 실험 전에 대조 arm 하나를 재측정해 기존 값과 대조한다.

## 6. 2차 백업분 (시간이 허락한 만큼; `BACKUP_MANIFEST.txt`가 최종 목록)

우선순위: ⑴ `results/`의 나머지 run (오래된 것부터가 아니라 **정본 문서가 참조하는
것부터** — excluded_runs·manifest 참조), ⑵ hf-cache. 점검 전 마지막 rsync pass가
EXP-107I 결과와 최종 커밋을 덮어썼는지 `BACKUP_MANIFEST.txt`의 시각으로 확인.
